name: my-api
# Your service is reachable at "http://my-api.${COPILOT_SERVICE_DISCOVERY_ENDPOINT}:3000" but is not public.
type: Backend Service

image:
  build: ./Dockerfile
  port: 3000

# Number of CPU units for the task.
cpu: 256
# Amount of memory in MiB used by the task.
memory: 512
# Number of tasks that should be running in your service.
count: 1
